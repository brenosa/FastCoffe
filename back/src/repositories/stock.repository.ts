import { Injectable } from "@nestjs/common";

@Injectable()
export class StockRepository {
    /* async updateStock(): Promise<Stock> { } */

    /* async getStock(idProduct: number): Promise<Stock> { } */

    /* async getStock(): Promise<Stock[]> { } */
}